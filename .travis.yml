language: python
python:
  - "2.7"
install:
  - pip install -r requirements-dev.txt --use-wheel
  - pip install -r requirements.txt --use-wheel
  - pip install -e . --use-wheel
  - pip install coveralls --use-wheel
before_script:
  - psql -c 'create database unicoremc_test;' -U postgres
script:
  - ./run_tests.sh
services:
  - elasticsearch
after_success:
  - coveralls
